<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ubsandroidapplication">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UBSAndroidApplication">
        <activity android:name=".ExchangeAnnoucement" />
        <activity
            android:name=".Exchange"
            android:label="@string/title_activity_exchange"
            android:theme="@style/Theme.UBSAndroidApplication.NoActionBar" />
        <activity android:name=".LoginResetSecurityQuestions" />
        <activity android:name=".LoginResetPassword" />
        <activity android:name=".homepage" />
        <activity android:name=".Search" />
        <!--
                <uses-library
                    android:name="com.google.android.wearable"
                    android:required="true" />

                       Set to true if your app is Standalone, that is, it does not require the handheld
                       app to run.

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />
        -->
        <activity
            android:name=".LoginForgotPassword"
            android:label="@string/title_activity_login_forgot_password">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginPage"
            android:label="@string/title_activity_login_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>